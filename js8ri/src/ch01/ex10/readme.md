# emptyEnumeration()
java.util.Enumerationにstaticメソッドとして入れる。

# emptyIterator()
java.util.Iteratorにstaticメソッドとして入れる。

# emptyList()
java.util.Listにstaticメソッドとして入れる。

# emptyListIterator()
java.util.ListIteratorにstaticメソッドとして入れる。

# emptyMap()
java.util.Mapにstaticメソッドとして入れる。

# emptyNavigableMap()
java.util.NavigableMapにstaticメソッドとして入れる。

# emptyNavigableSet()
java.util.NavigableSetにstaticメソッドとして入れる。

# emptySet()
java.util.Setにstaticメソッドとして入れる。

# emptySortedSet()
java.util.SortedSetにstaticメソッドとして入れる。

# nCopies(int n, T o)
java.util.Listにstaticメソッドとして入れる。

# newSetFromMap(Map<E,Boolean> map)
java.util.Setにstaticメソッドとして入れる。

# reverseOrder()
java.util.Xにdefaultメソッドとして入れる。(Xはjava.util内のコレクションインターフェイス)

# reverseOrder(Comparator<T> cmp)
java.util.Xにdefaultメソッドとして入れる。(Xはjava.util内のコレクションインターフェイス)

# singleton(T o)
java.util.Setにstaticメソッドとして入れる。

# singletonList(T o)
java.util.Listにstaticメソッドとして入れる。

# singletonMap(K key, V value)
java.util.Mapにstaticメソッドとして入れる。

# synchronizedCollection(Collection<T> c)
java.util.Collectionにstaticメソッドとして入れる。

# synchronizedList(List<T> list)
java.util.Listにstaticメソッドとして入れる。

# synchronizedMap(Map<K,V> m)
java.util.Mapにstaticメソッドとして入れる。

# synchronizedNavigableMap(NavigableMap<K,V> m)
java.util.NavigableMapにstaticメソッドとして入れる。

# synchronizedNavigableSet(NavigableSet<T> s)
java.util.NavigableSetにstaticメソッドとして入れる。

# synchronizedSet(Set<T> s)
java.util.Setにstaticメソッドとして入れる。

# synchronizedSortedMap(SortedMap<K,V> m)
java.util.SortedMapにstaticメソッドとして入れる。

# synchronizedSortedSet(SortedSet<T> s)
java.util.SortedSetにstaticメソッドとして入れる。

# unmodifiableCollection(Collection<? extends T> c)
java.util.Collectionにstaticメソッドとして入れる。

# unmodifiableList(List<? extends T> list)
java.util.Listにstaticメソッドとして入れる。

# unmodifiableMap(Map<? extends K,? extends V> m)
java.util.Mapにstaticメソッドとして入れる。

# unmodifiableNavigableMap(NavigableMap<K,? extends V> m)
java.util.NavigableMapにstaticメソッドとして入れる。

# unmodifiableNavigableSet(NavigableSet<T> s)
java.util.NavigableSetにstaticメソッドとして入れる。

# unmodifiableSet(Set<? extends T> s)
java.util.Setにstaticメソッドとして入れる。

# unmodifiableSortedMap(SortedMap<K,? extends V> m)
java.util.SortedMapにstaticメソッドとして入れる。

# unmodifiableSortedSet(SortedSet<T> s)
java.util.SortedSetにstaticメソッドとして入れる。
